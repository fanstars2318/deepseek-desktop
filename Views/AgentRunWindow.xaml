<Window x:Class="DeepSeekBrowser.Views.AgentRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeepSeek · Agent"
        Width="400" Height="500"
        MinWidth="360" MinHeight="320"
        WindowStartupLocation="Manual"
        Style="{StaticResource DsWindow}">
  <Grid Margin="{StaticResource DsDialogMargin}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,14">
      <TextBlock Text="Agent 工作台" Style="{StaticResource DsPageTitle}"/>
      <TextBlock Style="{StaticResource DsPageSubtitle}" Margin="0,0,0,0"
                 Text="127.0.0.1 Chat2API → 网页会话 · MCP 多步执行"/>
    </StackPanel>

    <Border Grid.Row="1" Style="{StaticResource DsCard}" Padding="12" Margin="0,0,0,10">
      <TextBlock x:Name="TaskText" TextWrapping="Wrap" Style="{StaticResource DsBody}"/>
    </Border>

    <Border Grid.Row="2" Style="{StaticResource DsCard}" Padding="8,4">
      <ListBox x:Name="LogList" Style="{StaticResource DsLogList}"/>
    </Border>

    <Border Grid.Row="3" Style="{StaticResource DsDialogFooter}" Margin="0">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="StopBtn" Content="停止" Click="Stop_Click" IsEnabled="False"
                Style="{StaticResource DsGhostButton}" MinWidth="72" Margin="0,0,8,0"/>
        <Button Content="隐藏" Click="Close_Click" Style="{StaticResource DsPrimaryButton}" MinWidth="72"/>
      </StackPanel>
    </Border>
  </Grid>
</Window>
