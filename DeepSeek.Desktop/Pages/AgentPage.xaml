<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DeepSeek.Desktop.Pages.AgentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <ToggleSwitch x:Name="DeepThinkToggle" Header="深度思考" IsOn="True" />
            <ToggleSwitch x:Name="WebSearchToggle" Header="联网搜索" />
            <ComboBox x:Name="StrategyBox" Header="策略" Width="140" SelectedIndex="0">
                <ComboBoxItem Content="Agent" Tag="react" />
                <ComboBoxItem Content="Plan" Tag="plan" />
            </ComboBox>
        </StackPanel>

        <ListView
            x:Name="MessageList"
            Grid.Row="1"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4">
                        <TextBlock FontWeight="SemiBold" Text="{Binding Role}" />
                        <TextBlock Text="{Binding Text}" TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBox
            x:Name="InputBox"
            Grid.Row="2"
            AcceptsReturn="True"
            PlaceholderText="描述你的任务…"
            TextWrapping="Wrap"
            MinHeight="72" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8">
            <Button x:Name="SendButton" Click="SendButton_Click" Content="发送" />
            <Button x:Name="StopButton" Click="StopButton_Click" Content="停止" />
        </StackPanel>
    </Grid>
</Page>
