# Legacy scripts（非 WPF 主线）

已移出 `scripts/` 根目录，仅供归档参考：

- Qt：`build-qt.ps1`、`install-dd-qt.ps1`、`Test-DdQtToolchain.ps1`、`verify-dd-ipc.ps1`
- TUI：`build-deepseek-tui.ps1`、`agent-tui-smoke.ps1`
- 一次性：`delete-deepseek-edge-repo.ps1`

日常构建请使用仓库根目录 `build.ps1`。
